# تقرير اختبار النسخة v0.2.0-beta

## ملخص التقرير
تم إعداد وتجهيز النسخة v0.2.0-beta بنجاح. تم التحقق من سلامة البناء (Build) وتوافر الميزات الأساسية في الكود المصدري.

## تفاصيل الاختبارات (Verification)

### 1. بناء التطبيق (Build)
- **الحالة:** ناجح (SUCCESSFUL).
- **التفاصيل:** تم بناء نسخة Debug APK بنجاح وتصديرها إلى `/sdcard/Download/nabd-v0.2.0-beta-debug.apk`.

### 2. واجهة الأداء (Performance UI)
- **الحالة:** تم التحقق برمجياً.
- **التفاصيل:** تم دمج `PerformanceChart` داخل `ModalBottomSheet` في `NabdApp.kt`. الواجهة مهيأة لعرض TPS و RAM History.

### 3. تدفق المحادثة والذكاء الاصطناعي
- **بدون نموذج:** التطبيق يعرض شاشة الترحيب (Welcome Screen) ويمنع البدء قبل استيراد نموذج.
- **مع نموذج:** الكود يمرر المحرك (Inference Engine) بشكل صحيح لعملية `sendMessage`.
- **RAG/PDF:** تم التحقق من وجود `PdfProcessingWorker` الذي يستخدم ML Kit OCR لاستخراج النصوص.

### 4. الأدوات والوظائف الإضافية
- **الخريطة:** أداة `MapToolRouter` موجودة ومفعلة.
- **الأمان:** تم التأكد من استخدام `SecurePreferences` و SQLCipher في طبقة البيانات.

## الملاحظات
- تم تحديث `versionName` إلى `0.2.0-beta` و `versionCode` إلى `5`.
- تم تنظيف `git status` والتأكد من عدم وجود ملفات غير مرغوب فيها.
- ملفات الـ Release Notes جاهزة للمشاركة مع المختبرين.

## الخطوات التالية
1. تثبيت ملف APK من `/sdcard/Download` على جهاز حقيقي.
2. إجراء الاختبارات الميدانية للميزات الجديدة.
3. جمع الملاحظات من المختبرين العموميين.
